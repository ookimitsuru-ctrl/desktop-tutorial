# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository state

This repository is currently the stock **GitHub Desktop tutorial** repo (created via GitHub Desktop's "create your first repository" flow). It contains no application source code, build tooling, package manifest, or test suite — only a placeholder `README.md`.

There are no build, lint, or test commands to run because no such tooling exists in the repo yet.

## Working here

- Since there is no established structure, language, or framework yet, do not assume conventions that aren't present — check with the user before introducing a stack (e.g. language, package manager, test framework) if the task doesn't specify one.
- Once real code is added to this repository, update this file to describe: the actual build/lint/test commands, and the high-level architecture/module layout — do not leave this file as stale boilerplate.
